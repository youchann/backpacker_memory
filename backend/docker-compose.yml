services:
  app:
    build:
      context: .
      dockerfile: "Dockerfile.dev"
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    environment:
      - GO_ENV=development
      - PORT=8080
    tty: true
    stdin_open: true
